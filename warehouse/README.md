# DuckDB Warehouse

This directory contains the DuckDB database file (`fxfill.duckdb`) used as the local OLAP engine.

**Note:** The `.duckdb` file is git-ignored. It will be created by running:

```powershell
python scripts/build_warehouse.py
```
