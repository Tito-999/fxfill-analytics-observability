# Architecture Diagram Source

The canonical generator for `architecture.svg` and `architecture.png` is:

`scripts/generate_portfolio_diagrams.py`

The generator derives model and page counts from the repository and must be
run whenever the warehouse or dashboard structure changes.
